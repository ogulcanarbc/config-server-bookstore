spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ogulcanarbc/book-store-configs.git
          username: gitusername
          password: gitpassword
          clone-on-start: true

server:
  port: 8888
